import React from 'react';
import PublicTemplate from '../templates/PublicTemplate'

const Main = (props) => {
    return <PublicTemplate>
        Recipio
    </PublicTemplate>
}

export default Main;